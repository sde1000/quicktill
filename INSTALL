Unpack the distribution (you have probably already done this if you're
reading this file).

To create a blank dataset, do:

createdb dbname
psql dbname
\i createdb

To delete the entire dataset (no questions asked!):
dropdb dbname

(createdb, psql and dropdb are PostgreSQL commands.)

